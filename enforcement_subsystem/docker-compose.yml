name: sleec-runtime-enforcer

services:

  rabbitmq:
    image: rabbitmq:3-management
    environment:
      RABBITMQ_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_PASS: ${RABBITMQ_PASS:-guest}
    ports:
      - "5672:5672"
      - "15672:15672"
    networks:
      - enforcer-network

  asmeta-server:
    build:
        context: ./asmeta_server
    ports:
      - "8080:8080"
    networks:
      - enforcer-network

  enforcer:
    build:
      context: ./enforcer
    environment:
      SERVER_IP: asmeta-server
      RABBITMQ_HOST: ${RABBITMQ_HOST:-rabbitmq}
      RABBITMQ_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_PASS: ${RABBITMQ_PASS:-guest}
      MODEL: ${MODEL}
      MODEL_HEADER: ${MODEL_HEADER}
    depends_on:
      - asmeta-server
    networks:
      - enforcer-network

  ari-sim-comm-layer:
    profiles: ["ari-sim", "ari-sim-test"]
    build:
      context: ./ros2_ws
    environment:
      RABBITMQ_HOST: ${RABBITMQ_HOST:-rabbitmq}
      RABBITMQ_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_PASS: ${RABBITMQ_PASS:-guest}
    entrypoint: "/bin/bash -c 'source /opt/ros/humble/setup.bash && source /ros2_ws/install/setup.bash && ros2 launch ari_sim_comm_layer ari_sim_comm_layer_launch.py rabbitmq_host:=${RABBITMQ_HOST:-rabbitmq} rabbitmq_user:=${RABBITMQ_USER:-guest} rabbitmq_pass:=${RABBITMQ_USER:-guest}'"
    networks:
      - enforcer-network

  ari-sim:
    profiles: ["ari-sim"]
    build:
      context: ./ros2_ws    
    entrypoint: "/bin/bash -c 'source /opt/ros/humble/setup.bash && source /ros2_ws/install/setup.bash && ros2 launch ari_sim ari_sim_launch.py'"
    networks:
      - enforcer-network

  ari-sim-test-runner:
    profiles: ["ari-sim-test"]
    build:
      context: ../
      dockerfile: ./experiments/Dockerfile
    entrypoint: ["sleep", "infinity"]
    networks:
      - enforcer-network

networks:
  enforcer-network:
    # driver: bridge